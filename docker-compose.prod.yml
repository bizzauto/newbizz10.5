# ==============================================
# BizzAuto CRM - Production Stack
# Nginx (SSL) + App + DB + Redis + n8n + Evolution
# ==============================================

version: '3.8'

services:
  # ==================== NGINX (Reverse Proxy + SSL) ====================
  nginx:
    image: nginx:1.27-alpine
    container_name: bizzauto-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/letsencrypt:ro
      - uploads_data:/var/www/uploads:ro
      - certbot-www:/var/www/certbot:ro
    depends_on:
      - app
    networks:
      - bizzauto-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'

  # ==================== CERTBOT (SSL Auto-Renewal) ====================
  certbot:
    image: certbot/certbot:v3.0.1
    container_name: bizzauto-certbot
    volumes:
      - ./nginx/ssl:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --webroot -w /var/www/certbot --quiet; sleep 12h & wait $${!}; done;'"
    networks:
      - bizzauto-network

  # ==================== BIZZAUTO APP ====================
  app:
    build: .
    container_name: bizzauto-app
    restart: always
    expose:
      - "4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_ENABLED=true
      - REDIS_URL=${REDIS_URL:-redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - CORS_ORIGIN=${FRONTEND_URL}
      - FRONTEND_URL=${FRONTEND_URL}
      - VITE_API_URL=${API_URL}
      - EVOLUTION_API_URL=${EVOLUTION_API_URL:-http://evolution-api:8080}
      - EVOLUTION_API_KEY=${EVOLUTION_API_KEY}
      - EVOLUTION_INSTANCE_NAME=${EVOLUTION_INSTANCE_NAME:-bizzauto}
      - N8N_URL=${N8N_URL:-http://n8n:5678}
      - N8N_APP_API_KEY=${N8N_APP_API_KEY}
      - N8N_API_KEY=${N8N_API_KEY}
      - N8N_PASSWORD=${N8N_PASSWORD}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - GROK_API_KEY=${GROK_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - NVIDIA_NIM_API_KEY=${NVIDIA_NIM_API_KEY}
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER:-bizzautoai@gmail.com}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=bizzautoai@gmail.com
      - RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}
      - RAZORPAY_KEY_SECRET=${RAZORPAY_KEY_SECRET}
      - RAZORPAY_WEBHOOK_SECRET=${RAZORPAY_WEBHOOK_SECRET}
      - META_APP_ID=${META_APP_ID}
      - WHATSAPP_REDIRECT_URL=${WHATSAPP_REDIRECT_URL}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REDIRECT_URL=${GOOGLE_REDIRECT_URL:-https://bizzautoai.com/api/auth/google/callback}
      - GOOGLE_BUSINESS_REDIRECT_URL=${GOOGLE_BUSINESS_REDIRECT_URL:-https://bizzautoai.com/api/google-business/auth/callback}
      - GOOGLE_AUTH_REDIRECT_URL=${GOOGLE_AUTH_REDIRECT_URL:-https://bizzautoai.com/api/auth/google/callback}
      - VITE_GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - bizzauto-network
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          memory: 1024M
          cpus: '1.5'
        reservations:
          memory: 512M

  # ==================== REDIS ====================
  redis:
    image: redis:7-alpine
    container_name: bizzauto-redis
    restart: always
    command: redis-server --appendonly yes --save 60 1000 --save 300 10 --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required} --maxmemory 256mb --maxmemory-policy allkeys-lru
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep PONG"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - bizzauto-network
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'

  # ==================== EVOLUTION API (WhatsApp) ====================
  evolution-api:
    image: evoapicloud/evolution-api:3.1.0
    container_name: bizzauto-evolution
    restart: always
    expose:
      - "8080"
    environment:
      - SERVER_PORT=8080
      - SERVER_TYPE=http
      - AUTHENTICATION_API_KEY=${EVOLUTION_API_KEY}
      - AUTHENTICATION_EXCLUDE_INSTANCES=default
      - DATABASE_ENABLED=true
      - DATABASE_CONNECTION_URI=${EVOLUTION_DATABASE_URL:-${DATABASE_URL}}
      - DATABASE_SAVE_MESSAGES=true
      - DATABASE_SAVE_CONTACTS=true
      - DATABASE_SAVE_INSTANCES=true
      - REDIS_ENABLED=true
      - REDIS_URI=${REDIS_URL:-redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - WEBHOOK_GLOBAL_URL=${EVOLUTION_WEBHOOK_URL:-${FRONTEND_URL:-https://bizzautoai.com}/api/evolution/webhook}
      - WEBHOOK_GLOBAL_ENABLED=true
      - QRCODE_LIMIT=30
      - QRCODE_COLOR=#25D366
    volumes:
      - evolution_instances:/evolution/instances
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - bizzauto-network
    deploy:
      resources:
        limits:
          # Raised for multi-instance number rotation: each connected WhatsApp
          # session (Baileys) costs ~50-150MB inside this container.
          memory: 1024M
          cpus: '1.0'
        reservations:
          memory: 256M

  # ==================== n8n WORKFLOW AUTOMATION ====================
  n8n:
    image: n8nio/n8n:1.65.0
    container_name: bizzauto-n8n
    restart: always
    expose:
      - "5678"
    environment:
      - N8N_PORT=5678
      - N8N_HOST=${N8N_HOST:-localhost}
      - N8N_PROTOCOL=${N8N_PROTOCOL:-http}
      - N8N_METRICS=false
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - N8N_USER_MANAGEMENT_JWT_SECRET=${N8N_USER_MANAGEMENT_JWT_SECRET}
      - N8N_SECURE_COOKIE=false
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD}
      - WEBHOOK_URL=${N8N_WEBHOOK_URL:-https://bizzautoai.com}
      - GENERIC_TIMEZONE=Asia/Kolkata
      - DB_TYPE=postgres
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_USER=${POSTGRES_USER}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      - N8N_APP_URL=${FRONTEND_URL:-https://bizzautoai.com}
      - N8N_API_URL=${API_URL:-https://bizzautoai.com}
      - N8N_OPENROUTER_KEY=${OPENROUTER_API_KEY}
    volumes:
      - n8n_data:/home/node/.n8n
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5678/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - bizzauto-network
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'

networks:
  bizzauto-network:
    driver: bridge

volumes:
  redis_data:
  n8n_data:
  evolution_instances:
  uploads_data:
  certbot-www:
